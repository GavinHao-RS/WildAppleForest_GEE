# 野苹果林遥感主流程建模链扩展说明

时间戳：2026-04-11 15:26:00 +08:00

## 一、文档目的

本文件用于说明 [wildapple_localrun_python.ipynb](/d:/CODE/VS__Project/GEE/WildAppleForest_GEE/wildapple_localrun_python.ipynb) 在 baseline 跑通之后新增的主流程建模环节，包括：

- 样本清洗
- 时序拟合结果回并
- 候选特征表构建
- `J-M + RF + 相关性` 特征筛选
- 清洗后 baseline 模型与筛选后模型输出

这份说明的定位是“功能与产物说明”，不是实验记录。

## 二、本次新增的主流程环节

### 1. 样本清洗

新增位置：
- [wildapple_localrun_python.ipynb](/d:/CODE/VS__Project/GEE/WildAppleForest_GEE/wildapple_localrun_python.ipynb)

新增逻辑：
- 对原始抽样表 `df_raw` 进行数值化处理
- 过滤缺失 `class / sample_id / group_id` 的记录
- 按特征缺失率先过滤高缺失特征
- 按月序列完整度过滤低完整度样本
- 按整行缺失比例过滤不稳定样本
- 对缓冲区扩样导致的同组像元做数量上限控制
- 生成清洗后的样本表 `CLEAN_DF`

当前相关参数：
- `row_missing_ratio_threshold`
- `monthly_completeness_threshold`
- `feature_missing_ratio_threshold`
- `max_pixels_per_group`

设计目的：
- 控制缓冲扩样带来的样本泄漏和重复像元堆积
- 在正式特征筛选前先提高样本质量

### 2. 时序长表与三次拟合

新增逻辑：
- 从 `CLEAN_DF` 生成按月长表 `LONG_DF`
- 针对每个 `sample_id × feature_name` 做三次多项式拟合
- 输出拟合系数：
  - `a3`
  - `a2`
  - `a1`
  - `a0`
  - `r2`
- 将拟合结果透视成宽表 `CUBIC_WIDE_DF`
- 回并到清洗后的主样本表，形成候选特征表 `CANDIDATE_DF`

设计目的：
- 将原始月序列特征与拟合形状特征同时纳入后续筛选
- 为后面的物候解释和单样本曲线诊断保留基础数据

### 3. 正式特征筛选

新增逻辑分三层：

1. 规则预筛
- 剔除高缺失特征
- 剔除近零方差特征

2. `Jeffries-Matusita` 关键类别对筛选
- 当前关键类别对：
  - `1 vs 2`
  - `1 vs 3`
  - `1 vs 4`
- 重点服务于 `Wild Apple Forest` 与主要混淆类之间的可分性评估

3. `RF + 相关性` 去冗余
- 先在 `J-M` 选出的特征子集上训练 grouped RF
- 用 RF 重要性进一步收缩候选集
- 对高相关特征做保序去冗余
- 形成最终筛选特征集

当前相关参数：
- `jm_top_k`
- `rf_top_k`
- `corr_threshold`

设计目的：
- 避免直接把全量多源时序特征塞进最终模型
- 在可分性、模型贡献度和共线性之间做平衡

### 4. 清洗后 baseline 与筛选后模型

新增模型输出分两类：

1. `cleaned_baseline`
- 在清洗后样本表上直接建 grouped RF
- 用于评估“样本清洗本身”带来的变化

2. `screened_model`
- 在正式筛选后的特征集上建 grouped RF
- 这是后续 SHAP、整图分类、面积统计更应该参考的本地模型版本

设计目的：
- 把“样本清洗效果”和“特征筛选效果”拆开看
- 为后续方法比较保留中间层结果

## 三、理论上会新增的输出文件

这些文件会在运行第 4 到第 6 部分代码单元后写入：
- [outputs/wildapple_localrun_2021](/d:/CODE/VS__Project/GEE/WildAppleForest_GEE/outputs/wildapple_localrun_2021)

### 1. 样本清洗输出

- `cleaning_summary.csv`
  - 清洗各阶段样本数与保留量摘要
- `clean_samples.csv`
  - 清洗后的建模样本表

### 2. 时序拟合与候选特征输出

- `monthly_long_table.csv`
  - 清洗后样本的月序列长表
- `cubic_fit_table.csv`
  - 每个 `sample_id × feature_name` 的三次拟合结果
- `cubic_feature_wide.csv`
  - 拟合系数宽表
- `candidate_feature_table.csv`
  - 原始清洗特征 + 拟合系数的联合候选表

### 3. 特征筛选输出

- `rule_screen_feature_summary.csv`
  - 规则预筛后的特征摘要
- `jm_feature_scores.csv`
  - 各特征在关键类别对上的 `J-M` 得分
- `screening_summary.csv`
  - 各阶段特征数量变化
- `jm_selected_features.csv`
  - `J-M` 选中的特征列表
- `rf_selected_features.csv`
  - RF 重要性截断后的特征列表
- `final_screened_features.csv`
  - 最终特征集
- `correlation_pruned_features.csv`
  - 相关性剪枝时被剔除的特征及其阻塞来源

### 4. 清洗后 baseline 模型输出

- `cleaned_baseline_classification_report.csv`
- `cleaned_baseline_confusion_matrix.csv`
- `cleaned_baseline_confusion_matrix.png`
- `cleaned_baseline_feature_importance.csv`
- `cleaned_baseline_feature_importance.png`

### 5. 筛选后模型输出

- `screened_model_classification_report.csv`
- `screened_model_confusion_matrix.csv`
- `screened_model_confusion_matrix.png`
- `screened_model_feature_importance.csv`
- `screened_model_feature_importance.png`

## 四、运行顺序

建议在主 notebook 中按以下顺序运行：

1. 已完成的 baseline 前半段
- 直到 `df_raw` 和 baseline 抽样成功

2. 第 4 部分
- 样本清洗
- 清洗后 baseline RF

3. 第 5 部分
- 时序长表
- 三次拟合
- 候选特征表

4. 第 6 部分
- 规则预筛
- `J-M` 排序
- RF 重要性筛选
- 相关性去冗余
- 最终筛选模型

## 五、当前应如何解读结果

如果这些单元成功跑通，说明主 notebook 已经从：

- “baseline 抽样 + baseline RF”

推进到了：

- “清洗后建模 + 拟合特征回并 + 正式筛选 + 最终本地筛选模型”

但这仍然不是完整终点。后续还缺：

- SHAP
- 物候标准差包络图
- 单样本真实散点与拟合曲线图
- GEE 整图分类
- 空间平滑
- 面积统计

## 六、后续建议

当前最合理的下一步是：

1. 运行新增的第 4 到第 6 部分
2. 检查：
   - `cleaning_summary.csv`
   - `screening_summary.csv`
   - `final_screened_features.csv`
3. 判断清洗阈值和筛选参数是否需要收紧或放宽
4. 在筛选后模型稳定后，再进入 SHAP 和整图分类
