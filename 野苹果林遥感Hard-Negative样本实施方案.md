# 野苹果林遥感Hard-Negative样本实施方案

时间戳：2026-04-14 10:20:00 +08:00

## 一、方案定位

本方案用于将 hard negative 思路从概念层落到主 notebook 的可执行样本策略中。

当前采用两阶段路线：

1. 第一阶段
   - 先通过规则方式构建一批高质量 hard negatives
   - 不依赖人工目视选点
   - 重点目标是压低 `class 1` 假阳性

2. 第二阶段
   - 在第一阶段样本策略跑通后
   - 再叠加“误分驱动”的 hard negative 挖掘

本文件讨论的是第一阶段的实施方案。

## 二、对当前用户方案的评估

你的方案是：

1. 减小 `class 1` 缓冲区和缓冲区内采样上限，控制正样本数
2. 对正样本构建 ring
3. 根据之前分类得到的混淆矩阵，进行易混类筛选
4. 生境约束过滤新增的负样本，海拔、坡度、距谷地距离等
5. 控制新增负样本数

这个方案是合理的，而且顺序基本正确。

### 为什么这个方案成立

#### 1. 先控制正样本规模，再补 hard negative

当前项目的突出问题不是“负样本完全不够”，而是：
- `class 1` 缓冲扩样后数量明显偏大
- 这会提高模型把相似地物吸进 `class 1` 的倾向

因此先收紧：
- `wildapple_buffer_m`
- `max_pixels_per_group`

是正确的起点。

#### 2. ring 比直接在 buffer 内采样更安全

直接在正样本 buffer 内找负样本，标签污染风险高。  
改成在正样本外围构造 ring，可以同时满足：
- 靠近正样本
- 又不直接落在正样本影响区内

这一步是当前方案的核心。

#### 3. 结合混淆矩阵筛选易混类是必要的

hard negative 不应该来自所有负类，而应该优先来自：
- 当前模型最容易和 `class 1` 混淆的类别

因此用上一轮分类的混淆矩阵来决定：
- 重点补 `Other Forest`
- 还是重点补 `Grassland/Shrub`
- 或者兼顾少量 `Cropland`

这是比“平均补 WorldCover 各类点”更合理的策略。

#### 4. 生境约束是必须的

只靠“离正样本近”不够，因为很多靠近正样本的负类并不一定真的是野苹果林混淆对象。

加入：
- 海拔
- 坡度
- 距谷地距离

能把负样本进一步限制在与野苹果林更相近的生态位上。

#### 5. 必须控制新增负样本数

如果不控数量，很容易从少数正样本周边挖出过多 hard negatives，导致：
- 局部区域被过度代表
- 训练集再次失衡

因此你方案里的第 e 步是必要的，不是可选项。

## 三、建议保留的总体流程

建议把第一阶段 hard negative 实施流程固定为：

1. 收紧正样本扩样
2. 构建正样本外环 ring
3. 基于混淆矩阵选择重点负类
4. 在 ring 内用 WorldCover 或等价标签构造候选点
5. 加入生境过滤
6. 控制每个正样本周边的新增负样本上限
7. 合并回训练样本，重新跑清洗与筛选链

## 四、建议的技术细化

## 1. 第一步：收紧 class 1 样本

当前建议优先调整两个参数：

- `wildapple_buffer_m`
- `max_pixels_per_group`

建议先做一版保守设置：

- `wildapple_buffer_m`
  - 从当前值下调一个档位
- `max_pixels_per_group`
  - 从当前值下调一个档位

这里不建议一次降得过猛。  
原因是：
- 如果正样本量降得太多，会直接削弱 `class 1` 识别能力
- 当前目标是压低假阳性，不是让 `class 1` recall 断崖式下滑

### 推荐原则

- 第一轮只做“适度收紧”
- 不做“彻底取消 buffer”

## 2. 第二步：构建 ring

每个 `class 1` 正样点构建两层缓冲：

- inner buffer
  - 代表正样本影响区
- outer buffer
  - 用于构造 ring 外边界

ring 的定义：

```text
ring = outer_buffer - inner_buffer
```

### ring 的作用

- 它靠近正样本
- 但避开正样本核心区
- 是 hard negative 候选区

### 推荐实现原则

- inner radius 要大于正样本最终保留的影响半径
- outer radius 不要过大

过大的 ring 会带来两个问题：
- 候选负样本不再“hard”
- 会把远距离、生态位差异很大的负样本也纳入进来

## 3. 第三步：基于混淆矩阵确定重点负类

不是所有负类都应该平权进入 hard negative 集。

建议根据上一轮模型混淆矩阵，先确定：
- `class 1` 最常被误分到哪些类
- 或哪些类最常被误判成 `class 1`

### 当前优先关注的类别

现阶段大概率优先级应是：

1. `Other Forest`
2. `Grassland/Shrub`
3. 视情况少量保留 `Cropland`

### 推荐策略

将 hard negative 候选主要限制在：
- `Other Forest`
- `Grassland/Shrub`

`Cropland` 可作为次一级补充，不建议第一轮就大量加入。

## 4. 第四步：ring 内候选点提取

在 ring 内生成候选负样本时，建议：

- 先从 WorldCover 中映射到当前项目类别体系
- 只保留目标的易混类类别值
- 在 ring 内做随机或分层采样

### 注意点

这里仍然不是最终 hard negative，还只是候选点。

因为它们还没有经过：
- 生境过滤
- 数量控制

## 5. 第五步：生境约束过滤

对 ring 内候选点，再加生境约束。

建议优先使用当前 notebook 已有的特征体系：

- `elevation`
- `slope`
- `dist_to_valley_proxy_m`

如果后续需要，也可以再加：
- `aspect`

### 推荐逻辑

不是拿固定绝对阈值硬切全部研究区，而是：
- 参考正样本生境分布范围
- 让候选负样本落在与正样本相近的生境区间内

例如可以采用：
- 按正样本生境分布的分位数范围进行过滤
- 或用“距正样本局部均值的容许偏差”进行过滤

### 原因

如果不用生境过滤，ring 内会混入很多：
- 空间近但生态位并不相似的负样本

这种样本更像“普通负样本”，不是 hard negative。

## 6. 第六步：控制新增负样本数

新增 hard negatives 必须有数量控制。

建议至少控制两层：

### 第一层：每个正样本 group 的新增负样本上限

避免某个 `class 1` 样点周围挖出过多候选负样本。

### 第二层：全局 hard negative 总量上限

避免整个训练集又因为新增负样本而失衡。

### 目标

当前这一步的目标不是让 hard negatives 在数量上压过原始负样本，而是：
- 让它们在结构上补足最难分的区域

## 五、建议新增到 notebook 的函数模块

如果要落地到主 notebook，建议新增以下模块：

### 1. `build_class1_ring_regions()`

作用：
- 从正样点生成 ring 区域

输入：
- 正样点
- inner radius
- outer radius

输出：
- ring polygon feature collection

### 2. `select_confusable_worldcover_classes()`

作用：
- 根据上一轮混淆矩阵，决定当前 ring 内只保留哪些类别

输出：
- 目标类别编码列表

### 3. `sample_ring_hard_negatives()`

作用：
- 在 ring 内从易混类中提取候选 hard negative 点

### 4. `filter_hard_negatives_by_habitat()`

作用：
- 对候选点按海拔、坡度、距谷地距离做过滤

### 5. `cap_hard_negative_count()`

作用：
- 控制每个正样本周边和全局的 hard negative 数量

### 6. `merge_hard_negative_samples()`

作用：
- 将新增 hard negatives 合并回现有训练样本表

## 六、建议的第一轮实验设置

第一轮 hard negative 实验不建议同时改太多东西。

建议只动以下几项：

1. 略微收紧 `class 1` 扩样
2. 增加一批 ring-based hard negatives
3. 只保留最重要的 1 到 2 类混淆类
4. 重新跑：
   - 样本清洗
   - 特征筛选
   - `screened_model`

### 第一轮重点观察指标

不要只看 OA。重点看：

- `class 1` precision
- `class 1` recall
- `class 1` 与 `Other Forest / Grassland-Shrub` 的混淆变化
- 最终面积是否更保守

## 七、第二阶段：误分驱动 hard negative

你的计划是：

- 第一轮先按规则方案跑一次
- 第二轮再做误分驱动 hard negative

这个顺序是对的。

原因：
- 先把静态规则版跑通，才能知道基础 hard negative 是否有效
- 如果一开始就加入误分驱动，变量太多，不利于解释效果来源

因此建议：

### 第一阶段

采用本文件方案：
- 收紧正样本
- ring
- 易混类
- 生境过滤
- 数量控制

### 第二阶段

在第一阶段基础上，再加入：
- 模型高置信误分区域挖掘
- 形成第二轮 hard negative 增强集

## 八、当前结论

你的方案没有方向性问题，而且已经接近可直接实施。

如果要进一步压缩成一句话，当前最推荐的实施路线是：

1. 适度收紧 `class 1` 缓冲扩样
2. 在正样本外构建 ring
3. 只在易混类中取候选负样本
4. 用海拔、坡度、距谷地距离过滤
5. 控制新增负样本数
6. 先跑一轮静态规则版
7. 再跑误分驱动增强版

这条路线最符合当前项目的两个目标：

- 降低 `class 1` 假阳性
- 避免后续面积估计虚高
