# 野苹果林遥感Hard-Negative样本策略说明

时间戳：2026-04-13 10:28:00 +08:00

## 一、文档目的

本文件用于说明在不通过人工目视选点的前提下，如何为野苹果林分类任务自动构建更有针对性的 hard negative 样本。

这里的 hard negative 指：
- 不是随机负样本
- 而是那些在空间位置、生态生境或遥感表现上更容易与 `class 1` 野苹果林混淆的负样本

这份文档重点讨论：
- 为什么不建议直接在 `class 1` buffer 覆盖内选负样本
- 更稳的自动采样思路是什么
- 后续如何把这套思路落实到 notebook 中

## 二、什么是 hard negative

在当前任务里，hard negative 主要是指那些：
- 标签上属于非野苹果林
- 但在模型上很容易被误分为野苹果林

典型对象包括：
- 其他森林
- 草地/灌丛
- 谷地附近的相似林分
- 局部与野苹果林光谱、SAR 或地形条件相近的地块

它们和普通负样本的区别在于：
- 普通负样本更容易分类
- hard negative 更接近模型真正的混淆来源

## 三、为什么不建议直接在 class 1 buffer 覆盖内选负样本

一种直觉上的做法是：
- 在野苹果样本点的 buffer 覆盖区内
- 从 WorldCover 的易混类别中选负样本

这个想法方向上是合理的，因为它试图“靠近正样本”去找难负样本。  
但直接在 `class 1` buffer 内选，风险较大。

### 主要风险

1. 容易抽到真实正样本边缘
- `class 1` buffer 本身就是正样本影响区
- 里面很可能仍包含真实野苹果林像元

2. 容易抽到混合像元
- 野苹果林边界、过渡带和小斑块周边往往混合严重
- 这些像元标签纯度不高

3. 会向训练集引入高噪声负样本
- 表面上看是 hard negative
- 实际上可能是“被错误标成负类的正样本或半正样本”

因此：
- 直接在 `class 1` buffer 内采负样本，风险高于收益

## 四、更稳的自动 hard negative 构造方式

## 1. 外环 ring 采样，而不是 buffer 内采样

更合理的思路是：
- 以 `class 1` 样点为中心
- 先有一个正样本 buffer
- 再在它外围构造一个 ring 区域

例如：
- 内半径：略大于正样本 buffer
- 外半径：再向外扩若干米

这样采样的好处是：
- 空间上靠近正样本
- 仍能保留“难负样本”性质
- 但比 buffer 内采样更不容易污染标签

### 核心思想

不是：
- 在正样本影响区里找负样本

而是：
- 在正样本邻近区里找负样本

## 2. 只从易混类别中采样

不建议从所有 WorldCover 类别中平均采样 hard negative。

更合适的是只在这些类别里找候选点：
- 其他森林
- 草地/灌丛
- 部分耕地边缘

原因：
- 这些类别才是后续最容易与 `class 1` 混淆的对象
- 水体、裸地等类别虽然也属于负类，但不是 hard negative 的重点来源

## 3. 叠加生态生境约束

如果只是“空间上接近正样本”，仍然不够。

更稳的方式是再叠加生境条件，使 hard negative 同时满足：
- 与野苹果样本距离较近
- 海拔相近
- 坡度相近
- 距谷地距离相近

这样得到的样本更像：
- 生态位上相似
- 但类别上不同

这类样本更适合训练模型区分“真正难分的非目标类”。

## 4. 保留安全带，避免贴边

即使使用 ring，也建议保留内圈安全带。

原因：
- 贴着正样本边界的位置，混合像元风险依然很高

因此 ring 设计时建议：
- 内圈不要太小
- 不要让 hard negative 直接贴着正样本 buffer 的边缘

## 五、推荐的自动 hard negative 采样流程

推荐流程如下：

### 第一步：构建正样本 buffer

围绕每个 `class 1` 样点构建正样本 buffer，用于定义正样本影响区。

### 第二步：构建外环 ring

在正样本 buffer 之外，再构建一个 ring：
- ring = outer_buffer - inner_buffer

ring 用来作为 hard negative 候选区。

### 第三步：在 ring 内叠加 WorldCover

只保留那些落在易混类别中的位置，例如：
- 其他森林
- 草地/灌丛

### 第四步：叠加生境过滤

根据已有地形/生境特征做约束，例如：
- 海拔在正样本局部范围附近
- 坡度接近
- `dist_to_valley_proxy_m` 接近

### 第五步：按 group 控制 hard negative 数量

不要让某一个正样本周边采出过多 hard negative。

应当控制：
- 每个正样本或每个 ring 采样数量上限

目的：
- 防止局部区域被过度代表

## 六、比单纯 WorldCover 更强的一步：误分驱动 hard negative

如果只依赖 WorldCover ring 采样，这仍然是静态方法。

更强的做法是在后续加入第二阶段：

### 第一阶段

使用：
- ring + WorldCover + 生境约束

得到第一批自动 hard negative。

### 第二阶段

在训练出当前模型后，再去找：
- 被模型高概率判成 `class 1`
- 但不在正样本区内
- 且落在 `Other Forest / Grassland-Shrub` 区域的样本

这些就是“误分驱动 hard negative”。

它们的优势是：
- 直接对应当前模型的弱点
- 比静态规则选点更贴近真正的误分来源

## 七、当前项目中的最优建议

对当前野苹果林项目，优先建议采用下面的路线：

### 基础版

- 不在 `class 1` buffer 内采样
- 使用 `class 1` 外环 ring 采样
- 只保留易混类别
- 加入海拔、坡度、谷地距离等生境过滤

### 增强版

在基础版完成后，再叠加：
- 模型误分驱动的 hard negative 挖掘

## 八、当前不建议的做法

以下做法当前不建议直接采用：

1. 在 `class 1` buffer 内直接抽负样本
2. 无差别增加所有 WorldCover 类别样本
3. 不做生境过滤，只看空间距离
4. 不控制每个正样本周边的 hard negative 数量

这些做法的问题分别是：
- 标签纯度风险高
- hard negative 针对性不够
- 生态位约束不足
- 容易引入局部样本堆积

## 九、后续落地到 notebook 时应重点加入的内容

如果后续要把这套策略落进 notebook，建议新增这些模块：

1. `build_hard_negative_ring()`
- 构建正样本外环候选区

2. `sample_worldcover_hard_negatives()`
- 在 ring 内按 WorldCover 易混类取样

3. `filter_hard_negatives_by_habitat()`
- 按海拔、坡度、谷地距离过滤

4. `cap_hard_negative_per_group()`
- 限制每个正样本周边保留的 hard negative 数量

5. 后续增强：
- 加入误分驱动 hard negative 迭代模块

## 十、一句话结论

如果不依赖人工目视选点，当前最稳妥的 hard negative 自动构造方式不是：

- 在 `class 1` buffer 内选负样本

而是：

- 在 `class 1` buffer 外环中
- 从易混类别中
- 结合生境约束
- 有控制地采样 hard negative

后续再用模型误分结果继续迭代增强。
