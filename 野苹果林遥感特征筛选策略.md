# 野苹果林遥感特征筛选策略

用于记录当前野苹果林分类任务中，多源遥感特征的筛选原则、筛选流程与推荐保留方向。该文档服务于后续 [wildapple_localrun_python.ipynb](d:/CODE/VS__Project/GEE/WildAppleForest_GEE/wildapple_localrun_python.ipynb) 的正式实现，也与 [野苹果林遥感采样调试记录.md](d:/CODE/VS__Project/GEE/WildAppleForest_GEE/野苹果林遥感采样调试记录.md) 中关于 Sentinel-1 调试的结论保持一致。

---

## 一、筛选目标

当前任务不是一般意义上的土地覆盖分类，而是以 `Wild Apple Forest` 为核心目标类的山区多分类识别。  
因此特征筛选的目标不是单纯压缩维度，而是同时满足以下三点：

- 提高 `Wild Apple Forest` 与易混类别之间的可分性
- 控制特征冗余，避免模型被高相关时序特征拖慢或误导
- 保留足够的生态解释性，方便后续做 SHAP、物候解释和生境解释

对本项目而言，最关键的易混关系通常不是“水体 vs 裸地”，而是：

- `Wild Apple Forest vs Other Forest`
- `Wild Apple Forest vs Grassland/Shrub`
- `Wild Apple Forest vs Cropland`

因此后续筛选必须围绕这些关键类别对展开，而不能只看整体分类精度。

---

## 二、为什么 Sentinel-1 先按轨道拆分再筛选

当前 debug 结论表明，山区内的 Sentinel-1 不适合在月尺度上直接把 `ASCENDING` 和 `DESCENDING` 影像混在一起合成。

原因是：

- 升轨与降轨的观测几何不同
- 山区坡向、阴影、叠掩与局部地形响应会导致系统性差异
- 如果先混合再做月序列，时序特征中会掺入轨道方向差异，而不只是地物变化

因此当前策略不是先“合并升降轨”，而是：

- 先按轨道方向保留
- 后续让筛选与模型决定保留哪一部分

这种做法的优点：

- 不会过早丢失可能有效的信息
- 可以明确比较 `ASC` 与 `DESC` 哪个更稳定
- 有利于后续解释特征到底来自哪种观测几何

对应到特征层面，Sentinel-1 建议优先保留为：

- `VV_db_ASC_*`
- `VH_db_ASC_*`
- `VV_VH_ratio_ASC_*`
- `VV_minus_VH_db_ASC_*`
- `VV_db_DESC_*`
- `VH_db_DESC_*`
- `VV_VH_ratio_DESC_*`
- `VV_minus_VH_db_DESC_*`

而不是在月尺度一开始就混成一套共同特征。

---

## 三、特征筛选总流程

建议采用三层筛选：

1. 规则型预筛选
2. 类间可分性筛选
3. 模型型筛选

这三层不是互相替代，而是逐层缩小候选空间。

---

## 四、第一层：规则型预筛选

这一层的目标不是直接选出最优特征，而是先剔除明显不合理、不稳定或高度冗余的变量。

### 1. 删除定义不正确或物理意义不稳定的特征

例如：

- 如果某个 SAR 特征在 dB 域和线性域混用，定义不严谨，应先修正再参与筛选
- 如果某个特征只是错误写法下的派生量，不应直接纳入模型

当前对 Sentinel-1 的建议是：

- `VV_db` / `VH_db` 必须使用正确的 dB 处理，不允许二次 `log10()`
- `VV_VH_ratio` 必须在线性域计算
- 如果要在 dB 域表达极化差异，更建议使用 `VV_minus_VH_db`

### 2. 删除长期大面积缺失的特征

若某个 band 在样本区中长期 sample count 明显不足，应优先删除或重构。  
否则它会拖垮训练样本数量。

### 3. 删除极低方差特征

如果一个特征在训练样本中几乎不变，它对分类帮助很小，通常应直接剔除。

### 4. 删除明显重复表达

例如：

- 同一物理含义的别名 band
- 仅命名不同但数值等价的变量
- 在当前阶段没有额外信息增益的重复特征

### 5. 对高时间相关特征先做粗压缩

比如某些相邻月份同类特征高度相关：

- `VV_db_ASC_M05`
- `VV_db_ASC_M06`
- `VV_db_ASC_M07`

在进入更细筛选前，可以先保留完整集合，但标记为“高相关组”，方便下一层处理。

---

## 五、第二层：类间可分性筛选

这一层重点使用 `Jeffries-Matusita (J-M) distance` 或类似类间可分性指标。

### 1. 不只看全局平均可分性

对本项目而言，更重要的是关键类别对之间的分离效果，而不是全局平均效果。

建议重点计算：

- `Wild Apple Forest vs Other Forest`
- `Wild Apple Forest vs Grassland/Shrub`
- `Wild Apple Forest vs Cropland`

### 2. 筛选方式建议

对每个候选特征：

- 计算关键类别对上的 J-M 距离
- 记录其在多个关键对中的稳定性
- 优先保留在多个关键对上都表现较好的特征

### 3. 对 Sentinel-1 的预期

最终留下的 SAR 特征大概率不是“全部月份全部轨道都保留”，而是：

- 某些关键月份的 `VV_db_ASC`
- 某些关键月份的 `VH_db_DESC`
- 某些差值特征 `VV_minus_VH_db`
- 少量对森林结构和山区地形响应更稳定的特征

也就是说，分轨保留是为了先不丢信息，但真正进最终模型的很可能只有一部分。

---

## 六、第三层：模型型筛选

规则预筛和 J-M 之后，还需要交给模型再做一次验证。

原因是：

- 单个特征看起来可分，不代表组合后最优
- 有些特征单独一般，但和其他特征联合时会很有用

### 1. 推荐流程

1. 用预筛选 + J-M 后的候选特征集训练基线 `RandomForest`
2. 查看特征重要性
3. 按相关性对高相关特征分组
4. 每组仅保留更稳定、解释性更强的特征
5. 重新训练 RF
6. 比较精度是否下降

### 2. 高相关组的处理原则

例如：

- `VV_db_ASC_M05` 与 `VV_db_ASC_M06` 高相关
- `VH_db_ASC_M07` 与 `VH_db_DESC_M07` 高相关

处理时不要机械删除，而应结合：

- 关键类别对可分性
- RF 重要性
- 是否更容易解释
- 是否在空间上更稳定

### 3. 压缩停止条件

如果删去某组特征后：

- 整体精度明显下降
- `Wild Apple Forest` 的用户精度或生产者精度下降明显
- 混淆矩阵中野苹果林与其他森林混淆加剧

则说明压缩过头，应停止继续删减。

---

## 七、推荐保留的特征类别

最终建议优先保留三大类特征。

### 1. 光学物候骨架

建议优先围绕以下指数建立月序列统计或拟合特征：

- `NDVI`
- `EVI`
- `NDMI`
- `LSWI`

这些特征负责表达：

- 生长季节变化
- 冠层绿度变化
- 水分变化
- 物候峰值和振幅

### 2. SAR 结构补充

建议重点考虑：

- `VV_db_ASC`
- `VH_db_ASC`
- `VV_db_DESC`
- `VH_db_DESC`
- `VV_minus_VH_db`
- `VV_VH_ratio`

这里需要强调：

- `VV_VH_ratio` 应在线性域计算
- `VV_minus_VH_db` 更适合作为 dB 域差值特征
- 不建议再回到“把 ASC/DESC 混在一起”的简单做法

### 3. 生境约束特征

野苹果林具有明显的地形生境偏好，因此建议保留：

- `elevation`
- `slope`
- `aspect`
- `dist_to_valley_proxy_m`

这些特征对控制“看起来像森林但实际上不是野苹果林”的误判很重要。

---

## 八、不建议直接全部保留的特征

以下特征类型不建议不加筛选地全部进入最终模型：

### 1. 所有月份所有轨道的完整 Sentinel-1 特征

原因：

- 特征量过大
- 高度相关
- 更容易把观测几何差异和真实地物差异混在一起

### 2. 定义不规范的 SAR 派生特征

例如：

- dB 域直接相除的 ratio
- 二次 `log10()` 得到的伪 dB 特征

### 3. 过多相邻月份重复变量

如果某个时期连续月份的变量变化很小，通常不必把全部月份都塞进最终模型。

---

## 九、推荐实施顺序

建议后续正式实现时，按以下顺序推进：

1. 先在 debug notebook 中确认：
   - 分轨后的 `ASC` / `DESC` sample count 是否稳定
   - `VV_db` / `VH_db` / `VV_VH_ratio` / `VV_minus_VH_db` 的定义和输出是否正常
2. 回到主 notebook，建立“完整但合理”的候选特征集
3. 进行规则型预筛选
4. 进行关键类别对 J-M 可分性筛选
5. 进行 RF + 相关性去冗余筛选
6. 固化最终特征集
7. 再做 SHAP 和生态解释

这个顺序的核心原则是：

- 先保证特征定义正确
- 再做筛选
- 最后再做解释

---

## 十、当前结论

当前阶段的推荐做法是：

- 不要一开始就只保留 `ASC` 或只保留 `DESC`
- 先把两者分开保留
- 再通过三层筛选决定最终保留哪些 SAR 特征

这比“先混升降轨再做月序列”更稳，也更适合山区野苹果林分类任务。
