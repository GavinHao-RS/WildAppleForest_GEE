# 野苹果林遥感改造方案

基于已填写的需求文档，面向 `2021 年伊犁州` 的 `Wild Apple Forest 6 类分类`，对现有 `ili_demo3_debug.js` 思路进行迁移和重构，最终落地为 `本地 Jupyter Notebook + GEE Python` 工作流。

相关参考文件：
- [ili_demo3_debug.js](d:/CODE/VS__Project/GEE/ili_demo3_debug.js)
- [PyGEE_多源遥感作物分类.ipynb](d:/CODE/VS__Project/GEE/PyGEE_多源遥感作物分类.ipynb)
- [PyGee_作物分类纯py版本.ipynb](d:/CODE/VS__Project/GEE/PyGee_作物分类纯py版本.ipynb)
- [改造需求文档.md](d:/CODE/VS__Project/GEE/WildAppleForest_GEE/改造需求文档.md)

---

## 一、需求落地结论

### 1. 任务目标
- 研究区：伊犁州，ROI 从 `FAO/GAUL` 提取。
- 年份：先做 `2021` 单年份。
- 分类体系：6 类。
  - 1 Wild Apple Forest
  - 2 Other Forest
  - 3 Cropland
  - 4 Grassland/Shrub
  - 5 Urban/Bare
  - 6 Water/Snow/Ice
- 输出优先级：
  - 分类精度
  - 面积统计
  - 物候图件
  - 生态解释
  - SHAP

### 2. 最终推荐框架
- `主框架`：以 `PyGee_作物分类纯py版本.ipynb` 为主。
- `多源特征构建`：借用 `PyGEE_多源遥感作物分类.ipynb` 的 S2 + S1 + 物候统计 + 缓冲区扩样思路。
- `ili_demo3_debug.js` 的作用：
  - 保留其 6 类分类思想。
  - 保留其后处理和平滑逻辑。
  - 但不建议继续作为主运行形态。

### 3. 总体策略
- GEE 负责：
  - 影像预处理
  - 多源特征构建
  - ROI 内抽样
  - 分类图生成
  - 面积统计基础栅格/表导出
- 本地 Python 负责：
  - 样本表格化
  - 数据清洗
  - 时序拟合
  - J-M 距离筛选
  - RF 建模
  - 精度评价
  - SHAP 解释
  - 图件输出

---

## 二、改造清单

## 1. 必须改

### A. 数据源改造
- 将原始以 S2 为主的特征体系改成 `S2 + S1` 联合。
- S2 至少保留：
  - blue, green, red, nir, swir1, swir2
  - NDVI, EVI, NDMI, NDWI/LSWI, GCVI
- S1 至少保留：
  - VV
  - VH
  - VV/VH
  - VV_db
  - VH_db

### B. 时间组织改造
- 以 `2021 年 3–10 月` 为生长季。
- 当前先采用 `月尺度合成`。
- 每个月输出一张光学与雷达合成影像。
- 在每个月的合成影像上提取指数序列。

### C. 物候特征体系改造
- 不再只用单季差值物候。
- 增加四类物候信息：
  - `时序统计特征`
  - `关键物候节点特征`
  - `时间序列拟合系数`
  - `物候波动特征`

建议至少包含：
- 均值、最大值、最小值、标准差、振幅
- 峰值月份
- 春季增速、秋季衰退速率
- 峰前差值、峰后差值
- 三次多项式拟合系数 `a0/a1/a2/a3`
- 拟合优度 `R2`

### D. 样本策略改造
- 现状样本按 `6 类各 150 个` 设计。
- 野苹果林样本来自 `实采 + GBIF`。
- 非目标类样本继续从 `ESA WorldCover` 和辅助规则中构建。
- 样本扩增采用：
  - 对高可信点样本做小尺度缓冲
  - 在缓冲区内提取像元
  - 但训练/验证划分必须按原始样点 ID 分组

### E. 本地建模链路改造
- 不再只用 GEE 端 `smileRandomForest` 完成全部工作。
- 改为：
  - GEE 抽样
  - geemap 转 DataFrame
  - 本地清洗
  - 本地拟合
  - J-M 距离筛选
  - 本地 RF
  - geemap.ml 回推到 GEE 生成分类图

### F. 精度评价改造
- 必须输出：
  - OA
  - Kappa
  - 混淆矩阵
  - 用户精度
  - 生产者精度
  - F1

### G. 结果图件改造
- 必须输出：
  - 分类图
  - 混淆矩阵图
  - 特征重要性图
  - SHAP 全局条形图
  - SHAP beeswarm
  - 物候标准差包络图
  - 真实散点与拟合曲线对比图
  - 面积统计图

### H. 后处理改造
- 平滑目标不是“图面更圆滑”，而是 `不要虚增野苹果林面积`。
- 后处理应采取保守策略：
  - 轻度多数滤波
  - 小斑块剔除
  - 重新掩膜
  - 必要时对野苹果林设置更严格的保留规则

---

## 2. 建议改

### A. 加入地形与生境约束
- 必加：
  - elevation
  - slope
  - aspect
  - distance to valley

原因：
- 野苹果林在伊犁分布具有较强生境选择性。
- 这类变量对减少“其他林地/果园误分为野苹果林”很有帮助。

### B. 加入纹理特征
- 建议在关键时期影像上做纹理。
- 优先用：
  - NDVI 纹理
  - 灰度纹理
- 不建议一开始纹理全量塞入，后续要做筛选。

### C. 增加野苹果林样本纯度控制
- 对野苹果林点样本，建议先做：
  - 高分影像抽查
  - 缓冲区纯度判断
  - 异常时序样本剔除

### D. 采用“保守判别”策略
- 你的目标更偏向“面积不能虚高”。
- 因此建议模型阈值与后处理都偏保守：
  - 宁可漏分少量边缘样地
  - 不要轻易把其他果林并进野苹果林

---

## 3. 可选增强

### A. 升降轨 SAR 分开统计
- 如果后续发现 S1 区分力不足，可以分 `ascending/descending` 统计。

### B. 多年迁移验证
- 当前先做 2021。
- 稳定后再做 2020/2022 年对比，检验物候和样本可迁移性。

### C. 分区面积统计
- 后续可以扩展为：
  - 按行政区统计
  - 按海拔带统计
  - 按坡向统计

---

## 三、技术路线

## 阶段 1：GEE 端多源影像准备

### 1. Sentinel-2 预处理
- 数据集：`COPERNICUS/S2_SR_HARMONIZED`
- 处理内容：
  - 云掩膜
  - 波段缩放
  - ROI 裁剪
  - 月尺度合成

### 2. Sentinel-1 预处理
- 数据集：`COPERNICUS/S1_GRD`
- 处理内容：
  - VV, VH
  - db 变换
  - VV/VH 比值
  - ROI 裁剪
  - 月尺度合成

### 3. 地形与生境层
- DEM
- slope
- aspect
- valley distance

这里的 `distance to valley` 建议实现为：
- 先确定谷地近似代理层，再做距离变换。
- 简化做法：
  - 以低坡位 + 河谷水系/低高程带联合定义 valley proxy
  - 再计算欧氏距离

如果后续要更稳，可以单独替换 valley 层数据源。

---

## 阶段 2：物候与多源特征构建

### 1. 月尺度时间序列
- 2021-03 到 2021-10，共 8 期。
- 每期构建：
  - 光学波段
  - 光学指数
  - SAR 波段
  - SAR 派生指标

### 2. 时序统计特征
- 对每个指数或波段沿时间维度计算：
  - mean
  - std
  - min
  - max
  - amplitude

优先对象：
- NDVI
- EVI
- NDMI
- LSWI
- VV
- VH
- VV/VH

### 3. 关键物候参数
- 对野苹果林这种季节性明显目标，建议增加：
  - `peak value`
  - `peak month`
  - `spring rise`：春季上升幅度
  - `autumn decline`：秋季衰退幅度
  - `summer minus spring`
  - `summer minus autumn`

### 4. 拟合特征
- 对月尺度序列做三次多项式拟合。
- 输出每个变量的：
  - a0
  - a1
  - a2
  - a3
  - r2

### 5. 纹理特征
- 在关键时期做纹理，建议优先选：
  - 7 月或 7–8 月合成影像
- 纹理对象：
  - NDVI
  - 灰度亮度

---

## 阶段 3：样本方案

## 1. 当前样本结构
- 6 类各 150 个，共 900 个。
- Wild Apple Forest：
  - 实采 + GBIF
- Other classes：
  - 主要由 WorldCover 辅助构建

## 2. 推荐样本流程

### 第一步：原始样点质检
- 检查野苹果林样点：
  - 是否落在明显非林地/城镇/裸地
  - 是否靠近斑块边界
  - 是否存在重复或极密集采样

### 第二步：野苹果林样本缓冲扩样
- 对野苹果林样点做小尺度缓冲。
- 建议先试：
  - `10 m`
  - `20 m`
- 不建议一上来 `30 m`，因为你这里是野苹果林，不是大面积纯农田作物。

### 第三步：缓冲区内抽样规则
- 不是直接把整个缓冲区所有像元都拿来训练。
- 而是：
  - 缓冲区内按固定数目抽点
  - 或 reduceRegions 提取代表像元
  - 并保留原始 `sample_id`

### 第四步：验证隔离
- 训练/验证必须按 `原始 sample_id` 分组。
- 也就是同一个原始样点缓冲出来的所有扩样像元，只能全部去训练集，或全部去验证集。

这一步很关键。否则模型会“见过同一地点附近的像元”，精度会虚高。

## 3. 为什么你之前说“不用避免同一缓冲区同时进训练验证”不合适

这就是 `空间自相关风险`：
- 遥感像元在空间上不是独立样本。
- 同一缓冲区内像元非常相似。
- 如果这些像元同时出现在训练集和测试集，模型并不是学会了“识别野苹果林”，而只是记住了“这个地点附近长什么样”。
- 结果会导致：
  - OA 偏高
  - F1 偏高
  - 面积结果在新区域或新年份上不稳定

因此你前面选择 `B` 是对的。

## 4. 非目标类样本建议

### Other Forest
- 不能只靠 WorldCover 随机采样。
- 需要额外控制，尽量覆盖：
  - 阔叶林
  - 一般果林
  - 河谷林

### Cropland
- 可继续用 WorldCover。
- 若后续混淆较少，可减少其样本密度。

### Grassland/Shrub
- 建议保留较多样本，因为容易和边缘野苹果林混。

### Urban/Bare, Water/Snow/Ice
- 可按规则样本构建，通常不是主要混淆源。

---

## 阶段 4：本地清洗、拟合与筛选

## 1. 数据清洗
- 删除无效值、缺失值。
- 剔除时间序列不完整样本。
- 剔除拟合失败样本。
- 对 SAR 缺失情况单独判断：
  - 若某些月份 SAR 缺失严重，可以对该变量不拟合或做补齐策略。

## 2. 为什么“光学 + SAR 一起拟合，后续再筛选”
- 先全部纳入，避免先验删掉有效信息。
- 再通过筛选压缩到稳定特征集。

## 3. 推荐筛选流程

### 第一步：J-M 距离筛选
- 对拟合系数特征和关键统计特征，计算类别间可分性。
- 先保留 `JM_mean` 高的特征。

### 第二步：相关性去冗余
- 对高相关特征删冗余。
- 例如绝对相关系数 > 0.9 时保留可解释性更强的一个。

### 第三步：RF 重要性再筛
- 在候选特征上训练本地 RF。
- 看全局重要性排名。
- 保留稳定贡献高的特征。

## 4. Jeffries-Matusita + RF 重要性联合筛选是什么意思

它不是一个单独算法，而是一套组合策略：

### J-M 距离做什么
- 衡量某个特征在不同类别之间的“可分性”。
- 如果一个特征对野苹果林和其他林地区分很强，它的 J-M 距离会高。

### RF 重要性做什么
- 衡量某个特征在模型里“实际有多大贡献”。
- 即使某个特征单独看可分性不错，如果和其他特征高度冗余，RF 重要性也可能不高。

### 联合筛选的好处
- `J-M` 负责先找“有区分力”的特征。
- `RF 重要性` 负责再找“在整体模型里真正有用”的特征。
- 最后得到的特征集更稳，也更容易解释。

---

## 阶段 5：本地模型与解释

## 1. 本地 RF
- 模型：`sklearn RandomForestClassifier`
- 输入：
  - 时序统计特征
  - 拟合系数
  - 地形因子
  - 纹理特征
  - 必要的关键期原始多源特征

## 2. 精度评价
- 输出：
  - OA
  - Kappa
  - Confusion Matrix
  - User's Accuracy
  - Producer's Accuracy
  - F1

## 3. SHAP 的两条线怎么区分

### 主线：本地 RF 样本模型 SHAP
- 对象：本地 RF。
- 数据：样本级特征表。
- 作用：
  - 解释哪些特征最能区分野苹果林
  - 解释不同类别的主导因子
  - 最适合输出全局条形图和 beeswarm

### 次线：像元级分类结果解释
- 对象：分类图背后的像元级输入特征。
- 不建议全图逐像元算 SHAP。
- 建议做法：
  - 从分类图中抽样代表像元
  - 对代表样本做 SHAP
  - 或做特征重要性映射 / 关键特征空间分布图

### 为什么分主次
- 本地 RF SHAP：
  - 稳
  - 快
  - 易出图
  - 易写论文解释
- 全图像元 SHAP：
  - 算得慢
  - 结果难控
  - 现阶段投入产出比低

---

## 阶段 6：制图、平滑与面积统计

## 1. 空间平滑原则
- 你的要求是“尽量不要增加野苹果林错误面积”。
- 因此平滑采用保守型。

推荐顺序：
- 轻度 majority filter
- 小斑块剔除
- 重新施加生境/掩膜限制

## 2. 生境约束后处理
- 若某些区域明显不可能分布野苹果林，可以做规则校正：
  - 超出合理海拔带
  - 远离谷地
  - 明显城镇/耕地错分

这一步要保守，不要过强。

## 3. 面积统计
- 先输出 6 类总面积。
- 单位：
  - m2
  - ha
  - ratio %

后续可扩展：
- 按海拔带
- 按坡向
- 按行政区

---

## 四、建议的最终 Notebook 结构

建议最终 Python Notebook 按下面组织：

### 1. 参数区
- 年份
- ROI
- 时间范围
- 月尺度设置
- 样本参数

### 2. GEE 初始化与 ROI

### 3. S2/S1 预处理函数

### 4. 月尺度合成

### 5. 指数计算与物候特征构建

### 6. 地形/谷地距离/纹理构建

### 7. 样本读取与缓冲扩样

### 8. GEE 抽样到 DataFrame

### 9. 数据清洗

### 10. 三次多项式拟合

### 11. J-M 距离筛选

### 12. RF 建模与精度评价

### 13. SHAP 解释

### 14. 物候标准差包络图

### 15. 真实散点 vs 拟合曲线图

### 16. geemap.ml 回写分类图

### 17. 空间平滑与面积统计

### 18. 最终图件导出

---

## 五、你问卷里几个术语的简明解释

## 1. 生长季时间序列统计特征
- 指不是只看某一个月，而是把整个生长季的一串值拿来统计。
- 例如：
  - NDVI_mean
  - NDVI_std
  - NDVI_max
  - NDVI_amp

它反映的是“这一年里整体长势和波动”。

## 2. 完整时间序列拟合
- 指把每个月的时序点当成一条曲线，用函数去拟合。
- 你这里采用三次多项式拟合最方便。

它反映的是“整条生长曲线的形状”。

## 3. 绿起始 / 峰值 / 衰退
- `绿起始`：春季开始快速返青的时间或拐点。
- `峰值`：长势最高的时期和值。
- `衰退`：秋季开始下降的时期和下降幅度。

这些很适合区分野苹果林与其他林地，因为物候节律不完全一样。

## 4. 标准差包络曲线
- 先算某类样本在每个时期的平均值。
- 再算每个时期的标准差。
- 画出 `均值 ± 标准差` 的带状曲线。

它反映的是：
- 该类的平均物候轨迹
- 该类内部的波动范围

## 5. 空间自相关风险
- 邻近像元相似，不独立。
- 同一缓冲区里抽出来的训练和测试样本会互相“泄漏信息”。
- 结果是精度被高估。

## 6. SHAP 的两类分析差异
- `本地 RF SHAP`：解释样本特征对模型分类的贡献。
- `像元级抽样 SHAP`：解释地图上某些典型像元为什么被分成某类。

前者偏全局机制解释，后者偏空间案例解释。

---

## 六、实施建议顺序

建议按这个顺序推进：

1. 先搭建 `2021 + 月尺度 + S2/S1 + 地形` 的基础特征栈  
2. 完成样本缓冲与分组划分  
3. 输出 GEE 样本表到本地  
4. 做清洗、拟合、J-M 筛选  
5. 训练本地 RF，先拿到第一版精度  
6. 再做 SHAP 和图件  
7. 最后做平滑和面积统计  

不要一开始就把所有高级图件和解释一起堆上去，先把第一版稳定分类结果跑通。

---

## 七、下一步建议

下一步最合理的工作顺序是：

### 方案 A
- 我先基于这个方案，给你输出一版 `Notebook 章节级实现计划`
- 每个章节对应需要写哪些函数、输入输出是什么

### 方案 B
- 我直接开始把 `ili_demo3_debug.js` 的关键逻辑迁移成 `本地 Python Notebook 骨架`
- 先搭好可运行框架，再逐段填充

如果你要我继续，建议直接走 `方案 B`。

