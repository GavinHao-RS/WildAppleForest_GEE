# 野苹果林遥感主流程关键参数说明

时间戳：2026-04-13 10:12:00 +08:00

## 一、文档目的

本文件用于说明 [wildapple_localrun_python.ipynb](/d:/CODE/VS__Project/GEE/WildAppleForest_GEE/wildapple_localrun_python.ipynb) 中 `CONFIG` 的关键参数，包括：

- 当前值
- 含义
- 调大或调小后通常会带来的影响

这份文档的定位是参数参考说明，不是实验记录。

## 二、基础时空参数

### `gee_project`

- 当前值：`kindle-400911`
- 含义：Earth Engine project ID，用于初始化 GEE 和访问资产
- 如何取值：必须填你当前可正常访问资产的 GEE 项目名

影响：
- 配错会导致 `ee.Initialize()` 或资产读取失败

### `year`

- 当前值：`2021`
- 含义：目标分析年份
- 如何取值：通常与样本调查年份和目标制图年份保持一致

调参影响：
- 改年份会直接改变 S2/S1 月合成、物候曲线和统计特征

### `start_month`

- 当前值：`3`
- 含义：时序分析起始月份
- 如何取值：一般取生长季开始前后

调大影响：
- 去掉过早月份，时序更短，可能减少噪声

调小影响：
- 引入更早季节信息，可能增强物候表达，也可能引入更多无效影像

### `end_month`

- 当前值：`10`
- 含义：时序分析结束月份
- 如何取值：一般覆盖到秋季衰退期

调大影响：
- 引入更晚季节信息，但也可能增加低质量月份

调小影响：
- 序列更短，可能丢失秋衰信息

### `scale`

- 当前值：`20`
- 含义：抽样和部分统计的空间尺度，单位米
- 如何取值：当前是 S2、S1、地形等多源特征的折中值

调大影响：
- 结果更平滑，计算更稳，但空间细节减少

调小影响：
- 空间细节更强，但噪声、计算量和混合像元敏感性会增加

### `random_seed`

- 当前值：`42`
- 含义：随机种子，用于抽样、group 内抽样和训练测试划分
- 如何取值：固定即可，主要用于可复现

## 三、样本与扩样参数

### `wildapple_asset`

- 当前值：`projects/kindle-400911/assets/wildapple_sample`
- 含义：野苹果样本资产路径
- 如何取值：必须是可访问的 GEE 样本资产

### `worldcover_points_per_class`

- 当前值：`150`
- 含义：WorldCover 每个非目标类的补样点数
- 如何取值：baseline 阶段 `100-200` 通常是合理范围

调大影响：
- 非目标类样本增多
- 如果补的是容易类别，收益有限

调小影响：
- 负样本更少，可能削弱非目标类约束

说明：
- 后续更推荐补充 hard negatives，而不是无差别提高所有类样本

### `enable_wildapple_buffer`

- 当前值：`True`
- 含义：是否对野苹果点样本做缓冲扩样
- 如何取值：`True` 或 `False`

调为 `False` 的影响：
- `class 1` 样本量下降
- 纯度通常更高
- 更适合做保守实验

调为 `True` 的影响：
- 样本量增加
- 但会增加空间自相关和混合像元风险

### `wildapple_buffer_m`

- 当前值：`20`
- 含义：野苹果样点缓冲半径，单位米
- 如何取值：取决于样点定位精度和样地纯度

调大影响：
- 扩样更多
- 更容易引入非目标像元

调小影响：
- 样本更保守
- 更有利于控制 `class 1` 假阳性

### `rect_sample_scale`

- 当前值：`30`
- 含义：矩形样地区域内规则采样的间隔尺度
- 如何取值：数值越小，样点越密

### `wildapple_rect_group_id`

- 当前值：`wildapple_rect_01`
- 含义：矩形样地规则采样样本的统一 group ID
- 作用：避免同一小样地在训练和测试中同时出现

### `wildapple_rect_corners`

- 当前值：4 个角点坐标
- 含义：矩形样地边界
- 作用：在指定样地范围内补充规则采样点

## 四、研究区和地形代理参数

### `adm0_name`

- 当前值：`China`
- 含义：GAUL 一级筛选中的国家名

### `adm1_name`

- 当前值：`Xinjiang Uygur Zizhiqu`
- 含义：GAUL 一级行政区名

### `adm2_name`

- 当前值：`Ili Kazakh`
- 含义：GAUL 二级行政区名，用于确定 ROI

### `valley_elevation_threshold_m`

- 当前值：`1800`
- 含义：谷地代理判定中的海拔阈值

调大影响：
- 谷地代理区域更宽
- `dist_to_valley_proxy_m` 的“谷地”定义更宽松

调小影响：
- 谷地代理更严格
- 距离特征更聚焦低海拔区域

### `valley_slope_threshold_deg`

- 当前值：`12`
- 含义：谷地代理判定中的坡度阈值

调大影响：
- 更多缓坡区域会被当作 valley proxy

调小影响：
- valley proxy 更接近真正低坡平缓区

## 五、S1 参数

### `s1_orbit_mode`

- 当前值：`asc_only`
- 含义：S1 轨道策略

可选逻辑：
- `asc_only`
- `desc_only`
- `separate`

当前为什么取 `asc_only`：
- 样本完整性最高
- 运行时间更短
- `DESC` 会压缩联合特征交集

### `s1_speckle_radius_pixels`

- 当前值：`1`
- 含义：S1 speckle filter 半径，单位像元

常见取值：
- `0`
- `1`
- `2`

调大影响：
- 平滑更强
- speckle 更弱
- 边界和细纹理可能被抹平

调小影响：
- 保留更多局部纹理
- 噪声也更明显

当前为什么取 `1`：
- 在样本数层面与 `0/2` 无明显差异
- 作为中等强度 baseline 更稳

## 六、后处理预留参数

### `postprocess_min_patch_pixels`

- 当前值：`150`
- 含义：后续整图分类后用于最小斑块筛除的阈值

调大影响：
- 结果更平滑
- 小斑块更容易被删除

调小影响：
- 保留更多细碎斑块
- 椒盐噪声可能更多

说明：
- 当前还没有正式接入整图分类后处理，但后面会用到

## 七、样本清洗参数

### `row_missing_ratio_threshold`

- 当前值：`0.35`
- 含义：单条样本记录允许的最大缺失比例

调大影响：
- 保留更多样本
- 脏样本也更容易留下

调小影响：
- 样本质量更严格
- 样本量会下降

### `monthly_completeness_threshold`

- 当前值：`0.85`
- 含义：月序列特征的最小完整度要求

调大影响：
- 只保留月序列更完整的样本

调小影响：
- 允许更多不完整时序进入模型

### `feature_missing_ratio_threshold`

- 当前值：`0.40`
- 含义：单个特征列允许的最大缺失比例

调大影响：
- 留下更多特征列
- 也可能保留较脏的列

调小影响：
- 特征列会更快被删减

### `max_pixels_per_group`

- 当前值：`25`
- 含义：每个 `group_id` 最多保留的像元数

调大影响：
- 保留更多缓冲扩样像元
- `class 1` 过量风险更高

调小影响：
- 更有效控制扩样样本堆积
- 更适合降低正类假阳性风险

说明：
- 这是当前最值得优先调整的样本平衡参数之一

## 八、特征筛选参数

### `jm_top_k`

- 当前值：`120`
- 含义：`J-M` 排序后保留的特征数量

调大影响：
- 保留更多候选特征
- 后续 RF 和相关性剪枝压力更大

调小影响：
- 更激进压缩
- 也更容易过早删掉有用特征

### `rf_top_k`

- 当前值：`60`
- 含义：在 `J-M` 子集上，根据 RF 重要性继续保留的特征数量

调大影响：
- 最终候选集更宽

调小影响：
- 更强压缩
- 可能损失互补信息

### `corr_threshold`

- 当前值：`0.95`
- 含义：相关性剪枝阈值

调大影响：
- 只去掉非常相似的特征
- 保留更多相关特征

调小影响：
- 更强去冗余
- 可能误删仍有独立价值的特征

## 九、当前哪些参数最值得后续优先调整

如果后续目标是：
- 降低 `class 1` 假阳性
- 让面积估计更保守

优先建议关注这些参数：

1. `wildapple_buffer_m`
2. `max_pixels_per_group`
3. `worldcover_points_per_class`
   - 或进一步替换为更有针对性的 hard negative 策略
4. `jm_top_k`
5. `rf_top_k`
6. `corr_threshold`
7. `postprocess_min_patch_pixels`

## 十、当前参数阅读建议

可以把这些参数分成三类理解：

### 1. 固定型参数

一般不需要频繁改：
- `gee_project`
- `year`
- `random_seed`
- `wildapple_asset`
- `adm0_name`
- `adm1_name`
- `adm2_name`

### 2. 任务定义型参数

会影响生态与时空表达：
- `start_month`
- `end_month`
- `scale`
- `valley_elevation_threshold_m`
- `valley_slope_threshold_deg`
- `s1_orbit_mode`
- `s1_speckle_radius_pixels`

### 3. 可调优型参数

后续最值得做实验：
- `wildapple_buffer_m`
- `worldcover_points_per_class`
- `max_pixels_per_group`
- `jm_top_k`
- `rf_top_k`
- `corr_threshold`
- `postprocess_min_patch_pixels`
