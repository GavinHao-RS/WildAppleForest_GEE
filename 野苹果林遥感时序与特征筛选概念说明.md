# 野苹果林遥感时序与特征筛选概念说明

时间戳：2026-04-11 15:40:00 +08:00

## 一、文档目的

本文件用于解释当前 [wildapple_localrun_python.ipynb](/d:/CODE/VS__Project/GEE/WildAppleForest_GEE/wildapple_localrun_python.ipynb) 中与时间序列建模和特征筛选相关的几个核心概念，包括：

- 时序曲线
- 物候统计特征
- 三次拟合
- `candidate_feature_table.csv`
- `jm_feature_scores.csv`
- `final_screened_features.csv`

这份文档的重点是定义、含义和作用，不讨论参数优劣。

## 二、时序曲线

### 1. 定义

时序曲线，是指某一个样本在多个时间点上的同一特征值变化轨迹。

在当前 notebook 中，时间点是：
- 2021 年 3 月到 10 月

因此，一个样本的某个特征会形成一条按月变化的序列，例如：
- `NDVI_M03`
- `NDVI_M04`
- ...
- `NDVI_M10`

把这些月份值按时间顺序连起来，就是该样本的 `NDVI` 时序曲线。

### 2. 含义

时序曲线反映的是：
- 某类地物在整个生长季中的变化节律
- 不是某一个单月的状态，而是一个连续过程

对于野苹果林分类来说，时序曲线的重要性在于：
- 野苹果林与其他林地、草灌、耕地在某一个月可能很接近
- 但在整个生长季的变化形态上通常不同

### 3. 作用

时序曲线的作用包括：
- 帮助识别关键月份
- 为物候统计提供原始基础
- 为后续多项式拟合提供输入
- 为后面的个体样本曲线诊断图提供数据来源

## 三、物候统计特征

### 1. 定义

物候统计特征，是从一条月序列曲线中提取出来的摘要性指标。

当前 notebook 已经生成的统计特征包括：
- `mean`
- `std`
- `min`
- `max`
- `amp`
- `peak_month`
- `spring_rise`
- `autumn_decline`

### 2. 含义

这些指标的含义如下：

- `mean`
  - 生长季平均水平
- `std`
  - 生长季波动强度
- `min`
  - 生长季最低值
- `max`
  - 生长季最高值
- `amp`
  - 振幅，即 `max - min`
- `peak_month`
  - 峰值出现的月份
- `spring_rise`
  - 春季到夏季的上升幅度
- `autumn_decline`
  - 夏季到秋季的回落幅度

### 3. 作用

物候统计特征的作用包括：
- 用少量统计指标概括整条曲线
- 提高特征可解释性
- 降低直接使用全时序宽表时的维度压力
- 帮助判断不同类别的季节响应差异

在生态解释上，这些特征比单个月份波段更容易说明问题。

## 四、三次拟合

### 1. 定义

三次拟合，是对某个样本在某个特征上的时序曲线，用三次多项式进行近似表达。

表达式形式为：

```python
y = a3*x^3 + a2*x^2 + a1*x + a0
```

其中：
- `x` 是时间偏移
- `y` 是该时间点的特征值

当前 notebook 会输出：
- `a3`
- `a2`
- `a1`
- `a0`
- `r2`

### 2. 含义

这些系数描述的是曲线整体形状，而不是某一个月的瞬时值。

可粗略理解为：
- `a0`
  - 基础水平
- `a1`
  - 一阶变化趋势
- `a2`
  - 曲率信息
- `a3`
  - 更复杂的非线性形态
- `r2`
  - 该曲线被三次多项式拟合的程度

### 3. 作用

拟合的作用是：
- 把整条曲线压缩成少量形状参数
- 让模型利用“曲线形态”而不只是“单月值”
- 为后面的物候解释和个体样本拟合图提供参数基础

因此，三次拟合特征可以看作是介于：
- 原始月序列
- 物候统计摘要

之间的另一层表达方式。

## 五、三个关键文件的定义与作用

## 1. `candidate_feature_table.csv`

### 定义

这是候选特征总表。

它是在清洗后的样本表基础上，回并三次拟合宽表后形成的联合特征表。

里面同时包含：
- 原始多源特征
  - 光学月序列
  - SAR 月序列
  - 物候统计特征
  - 地形特征
  - 纹理特征
- 拟合特征
  - 各类特征的 `a3/a2/a1/a0/r2`

### 含义

这个文件代表的是：
- 所有可能进入正式筛选和建模的候选特征池

它还不是最终特征集，只是正式筛选前的全集合。

### 作用

它的作用是：
- 作为后续特征筛选的输入
- 承接“原始特征 + 拟合形状特征”的联合建模
- 为后续查看特征空值、列数、结构提供检查对象

## 2. `jm_feature_scores.csv`

### 定义

这是每个候选特征在关键类别对上的 `Jeffries-Matusita` 可分性评分表。

当前 notebook 的关键类别对是：
- `1 vs 2`
- `1 vs 3`
- `1 vs 4`

即：
- 野苹果林 vs 其他森林
- 野苹果林 vs 耕地
- 野苹果林 vs 草地/灌丛

### 含义

这个文件反映的是：
- 某个特征对于区分目标类和关键混淆类到底有多大帮助

一般会包含：
- 单个类别对得分
- `jm_mean`
- `jm_min`

其中：
- `jm_mean`
  - 整体平均可分性
- `jm_min`
  - 在最难区分类别对上的最低表现

### 作用

它的作用是：
- 按生态学上更关键的类别对筛选特征
- 避免只依赖全局平均意义上的“好特征”
- 为后面的 RF 重要性筛选提供更有针对性的输入子集

## 3. `final_screened_features.csv`

### 定义

这是最终筛选后的特征清单。

它是在以下步骤之后得到的结果：
- 规则预筛
- `J-M` 排序截断
- RF 重要性截断
- 相关性去冗余

### 含义

这个文件代表的是：
- 当前 notebook 认为最值得保留进入正式本地模型的一组特征

它不再是原始候选池，而是压缩后的最终建模清单。

### 作用

它的作用是：
- 作为 `screened_model` 的输入特征列表
- 为后续 SHAP 提供更稳定的解释对象
- 为后续整图分类提供候选的最终特征方案

如果这个文件结构合理，通常会表现为：
- 不是所有月份都保留
- 不是所有特征类型都保留
- 同时保留少量高价值的光学、SAR、地形和拟合特征

## 六、三者之间的关系

这三个文件的关系可以概括为一条链：

1. `candidate_feature_table.csv`
   - 候选特征全集

2. `jm_feature_scores.csv`
   - 候选特征在关键类别对上的可分性排序

3. `final_screened_features.csv`
   - 最终留下来进入正式模型的特征清单

因此可以理解为：
- 第一个文件回答“有什么特征可以选”
- 第二个文件回答“哪些特征更有区分力”
- 第三个文件回答“最终实际保留了哪些特征”

## 七、当前阅读建议

运行主 notebook 后，建议按以下顺序看这些内容：

1. 先看 `candidate_feature_table.csv`
   - 确认拟合特征是否成功回并
   - 确认是否存在大量空列

2. 再看 `jm_feature_scores.csv`
   - 看目标类与关键混淆类之间，哪些特征更有区分力

3. 最后看 `final_screened_features.csv`
   - 看最终保留的是哪些类型的特征
   - 判断筛选结果是否过于偏向某一类特征

## 八、与后续流程的关系

这些概念和文件将直接影响：
- `screened_model`
- 后续 SHAP
- 后续整图分类
- 后续物候图件与样本曲线诊断

因此，这一部分不是中间性的“附属分析”，而是主流程建模链中的关键桥梁。
